// Export the domMax features set for lazy loading framer-motion.
// domMax includes all animation features (layout animations, drag, etc.),
// which are required for complex components like our AuthCard.
import { domMax } from "framer-motion";

export default domMax;
